/**
 * Перечисление типов биометирии
 */
export enum firTypeEnum {
    none = 0,                       // без беометрии
    face = 1,                       // отпечаток лица
    hand = 2,                       // льпечаток ладони
    fingerprint = 3                 // отпечаток пальца
}